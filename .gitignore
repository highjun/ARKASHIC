node_modules/
*.log
.output/
dist/
storybook-static/
.env
.env.*
.claude
docs/USER_NOTE.md

# 키·인증서 파일. 이름만 봐도 비밀인 것은 손이 닿기 전에 막는다.
*.pem
*.key
*.p12
*.pfx
*credential*

# CI 컨테이너용 가짜 값. `.env.*` 금지의 유일한 예외다 — 여기엔 시크릿이 없다.
!ops/deploy/.env.ci

# 복사해서 채우는 원본. 값이 없으므로 비밀이 아니다.
!ops/deploy/.env.example

# 파이썬 바이트코드 — tools/figma/ 가 파이썬을 쓴다
__pycache__/
